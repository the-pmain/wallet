# Example of an environment variables file. Copy to `.env` and fill in the values.
#
# Client selected when Vite starts or builds the application.
# This is a build-time value and is not exposed to browser code.
# Allowed values:
#   safe — the newer Safe client
#   etx  — the ETX client from main
THEME=safe
#
# ATTENTION. Vite inserts variables with the VITE_ prefix directly into the bundle.
# Everything that gets here will be available to anyone who opens the application sources.
# Secrets (private keys, mnemonics, API keys with the right signature) are here
# you CAN'T post it under any circumstances.

# The Alchemy API key is the default source of RPC nodes.
#
# THE WALLET WORKS WITHOUT A KEY: public nodes from the configuration are used
# networks. This is a working condition, not a bug.
#
# REQUIRED WHEN FILLING OUT. The key will be included in the bundle in plain text
# and will be visible to any user of the application. Limit it to the domain
# applications in the Alchemy panel (the "Allowlist" section of the application), otherwise the key
# will be used by outsiders and the quota will be exhausted in a matter of hours.
#
# ABOUT PRIVACY. One operator that handles all wallet requests sees
# The user's IP address and each address whose balance is being requested. This
# enough to link a personality to a briefcase. The user can specify
# own node in the network settings — then Alchemy is not used at all.
# VITE_ALCHEMY_API_KEY=

# Demo access key to CoinGecko — the source of courses for the portfolio.
#
# THE WALLET WORKS WITHOUT A KEY. Free public access takes one
# the address of the contract for the request (verified by contacting the service: failure 10012),
# therefore, courses are requested one at a time and slower. Addresses go away with the key
# in batches: fewer requests, fewer footprints, lower limit consumption.
#
# COURSES ARE NOT REQUESTED WITHOUT THE USER'S CONSENT. Having a key is nothing
# does not include: consent is asked on the briefcase screen with a listing
# of what exactly will come out.
#
# ABOUT PRIVACY. The token rate is requested at the address of its contract,
# in other words, the service learns the composition of the portfolio and the IP address. Wallet address
# not shared: the service does not know whose portfolio it is.
# VITE_COINGECKO_API_KEY=

# WalletConnect Project ID (Reown) — connecting applications.
#
# WITHOUT A KEY, THE SECTION OPENS and honestly reports that it is not configured;
# the library doesn't even load at the same time. The rest of the wallet is working.
#
# The key is public by the nature of the client application and does not grant signing rights:
# relay uses it to account for traffic. You can get it in the Reown panel.
#
# ABOUT PRIVACY. The Relay server sees your account addresses, metadata
 The # of each connected application and the time of each request. This is comparable
to a leak through the history indexer, so the connection is being performed
# by explicit user action, not by itself.
# VITE_WALLETCONNECT_PROJECT_ID=

# Simulation of transactions via Tenderly.
#
# ONLY FOR CHECKING ON YOUR OWN MACHINE. The access key gets into the text
 The # of the compiled program goes to everyone who opened the page: he gives
# the right to spend the quota of the project and read the history of its simulations.
# For the uploaded builds, the same data is entered in the wallet settings,
# where they are stored in encrypted storage and belong to the same owner.
#
# Without these values, the transaction consequence considers the node to be a method
# eth_simulateV1 is a working condition, not a limitation.
#
# Values are taken in Tenderly: Settings -> Authorization (key),
# project address of the type tenderly.co /<account>/<project>.
# VITE_TENDERLY_ACCOUNT=
# VITE_TENDERLY_PROJECT=
# VITE_TENDERLY_ACCESS_KEY=

# Address of the Node process (`npm run server:dev`). Used to insert a row
# into public.users. Empty = same origin via Vite proxy `/v1` → 127.0.0.1:8080.
# VITE_SERVER_URL=http://127.0.0.1:8080

# --- Node / Fastify (`npm run server:dev` / `npm start`) ---
# Vite never inlines these. They stay in the Node process.

NODE_ENV=development
HOST=127.0.0.1
PORT=8080

# Railway injects PORT. Leave HOST unset there (the process listens on 0.0.0.0).
# Root Directory of the service must be the repo root, not `server/`.
# Start command is in railway.json: `node server/src/index.ts`.
# Custom domain: set ALLOWED_ORIGINS to that https origin. Otherwise the
# process uses RAILWAY_PUBLIC_DOMAIN.

# Comma-separated origins allowed to call the API cross-origin.
# Required in production: the process will refuse to start without it.
ALLOWED_ORIGINS=

RATE_LIMIT_MAX=120
RATE_LIMIT_WINDOW_MS=60000
MAX_BODY_BYTES=65536
CATALOG_CACHE_SECONDS=300

# Supabase API URL, not the postgres URI.
#
# LOCAL DEVELOPMENT. Do not put a hosted project URL here and expect
# `npm run server:dev` to use it: in NODE_ENV=development the process
# refuses any SUPABASE_URL that is not 127.0.0.1 or localhost.
#
#   npm run local          # local DB + API + wallet UI
#   npm run local:db       # local DB only
#   npm run local:stop    # stop the local Docker stack
#
# `supabase:env` writes `.env.local` (gitignored) with the local API URL
# from `supabase status` (this repo: http://127.0.0.1:55321; Windows
# reserves the CLI default 54321) and the local anon / service-role keys.
# `.env.local` overrides these fields so production values that may
# already live in `.env` are not used by the local Node process.
SUPABASE_URL=

# Publishable key (preferred) or legacy anon key. Stays on the server.
# Never put these under VITE_ — they would land in the wallet bundle.
# Local: both are the anon key from `npm run supabase:env`.
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_ANON_KEY=

# Service-role key. Server-only. Bypasses RLS on public.users after the
# Node process authenticates the caller (email+the_p or admin password).
# Never put under VITE_, never return it from /v1, never log it.
# Local: the service-role key from `npm run supabase:env`, never a
# Dashboard production secret.
SUPABASE_SERVICE_ROLE_KEY=

# Directory of the built wallet (`index.html`). Empty = auto:
# `server/public`, then repo `dist/` after `npm run build`.
# STATIC_ROOT=

# Cloudflare Email Sending. Server-only. Never put these under VITE_:
# they would be compiled into the wallet bundle.
# Account ID: Cloudflare dashboard → any domain's Overview, right sidebar.
# API token (starts with cfut_ or cfat_): My Profile → API Tokens,
# permission Email Sending — Edit.
# A Global API Key (starts with cfk_) is not a token. It also needs
# CLOUDFLARE_EMAIL — the address used to log into dash.cloudflare.com.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_EMAIL=

# Default From address for outbound mail. Must be on a domain
# onboarded for Cloudflare Email Sending.
MAIL_FROM=support@etwalletx.com

# Password for `/admin`. Server-only. Never put under VITE_.
# The wallet bundle never sees this value. The Node process compares
# what the cabinet sends in `x-admin-pass` against this field.
# ADMIN_PASS — read-only: users, balances, sendings, and receivings. No writes.
ADMIN_PASS=
# SUPER_ADMIN_PASS — full cabinet: edit users, edit transfers, live stream.
SUPER_ADMIN_PASS=

# Comma-separated IP addresses allowed to call `/v1/admin`.
# Regular user routes are not restricted. Empty in development/test
# means no address check. Empty in production refuses every remote
# cabinet request. Loopback (127.0.0.1 / ::1) is always accepted.
# ALLOWED_ADDRESSES=185.238.203.103,185.238.203.200
ALLOWED_ADDRESSES=

# Shared secret for POST /v1/webhooks/email-inbound. Optional backup.
# Incoming mail is stored by the Cloudflare Email Routing worker in KV.
# EMAIL_WEBHOOK_SECRET=

